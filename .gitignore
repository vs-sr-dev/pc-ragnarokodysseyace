# --- BYOA policy: no proprietary asset in this repository ---
# See README.md. Code, tools and format documentation only.

*.iso
extract/

# Disc containers and everything that comes out of them
*.cpk
*.cpk.patch
*.pac
*.acb
*.awb
*.acf
*.pam
*.at3
*.CTEX
*.ctex
*.CMDL
*.CMTM
*.CNOM
*.CCLS
*.CSCN
*.CSCM
*.CTXT
*.PTP
*.psq
*.anmcmd
*.rmsg
*.col
*.map
*.mkc
*.mtm
*.trg
# ECH tables ship as .bin and the actor parameters as .json; both are game
# content. There is no hand-written file of either kind in this repository.
*.bin
*.json
*.otf
*.par
*.dat
*.DAT
EBOOT.BIN
PARAM.SFO
*.self
*.elf

# The disc index is derived from a specific disc and lists its layout;
# the manifest is a fingerprint (path, size, sha256) and IS publishable,
# which is what devilutionX and Ship of Harkinian do.
tools/iso_index.tsv

# Anything a tool here writes out of the disc: a dump of table values, a
# rendered frame, a mesh. The tool is publishable, its output is not.
generated/
*.png
*.obj
*.wav
*.hca
*.adx
*.vag
*.mpg

# Key material for the SELF decryption (Phase 3). Not game content, but not
# ours to redistribute either, and not derivable from the disc: it is a local
# input with exactly the standing the ISO has. `tools/self.py` takes the path
# to it as an argument, so the tool is publishable and the keys are not.
keys/
*.keys
# And a third-party source tree dropped in for the same purpose.
rpcs3-master/

# build
build/
out/
__pycache__/
*.pyc
